import styled from 'styled-components';

/**
 * Component helping to show tooltip on top of other elements that can't because of the pointer-events
 */
export const TooltipWrapper = styled.div`
  pointer-events: all;
`;

import styled from 'styled-components';

export const StatusDataBlockFieldsetStyle = styled.div`
  display: grid;
  height: 100px;
  grid-template: repeat(3, 1fr) / repeat(3, 1fr);
  box-sizing: border-box;
  flex-direction: column;
  row-gap: 24px;
`;

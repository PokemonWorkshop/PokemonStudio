import styled from 'styled-components';

export const TypeStyle = styled.div`
  #type {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 4px;
    width: 75px;
    font-size: 12px;
    font-weight: 400;
    line-height: 16px;
    user-select: none;
  }

  #type-normal {
    background: ${(props) => props.theme.colors.typeNormalSoft};
    color: ${(props) => props.theme.colors.typeNormalBase};
    border-radius: 100px;
  }

  #type-fire {
    background: ${(props) => props.theme.colors.typeFireSoft};
    color: ${(props) => props.theme.colors.typeFireBase};
    border-radius: 100px;
  }

  #type-grass {
    background: ${(props) => props.theme.colors.typeGrassSoft};
    color: ${(props) => props.theme.colors.typeGrassBase};
    border-radius: 100px;
  }

  #type-water {
    background: ${(props) => props.theme.colors.typeWaterSoft};
    color: ${(props) => props.theme.colors.typeWaterBase};
    border-radius: 100px;
  }

  #type-electric {
    background: ${(props) => props.theme.colors.typeElectricSoft};
    color: ${(props) => props.theme.colors.typeElectricBase};
    border-radius: 100px;
  }

  #type-ice {
    background: ${(props) => props.theme.colors.typeIceSoft};
    color: ${(props) => props.theme.colors.typeIceBase};
    border-radius: 100px;
  }

  #type-fighting {
    background: ${(props) => props.theme.colors.typeFightingSoft};
    color: ${(props) => props.theme.colors.typeFightingBase};
    border-radius: 100px;
  }

  #type-poison {
    background: ${(props) => props.theme.colors.typePoisonSoft};
    color: ${(props) => props.theme.colors.typePoisonBase};
    border-radius: 100px;
  }

  #type-ground {
    background: ${(props) => props.theme.colors.typeGroundSoft};
    color: ${(props) => props.theme.colors.typeGroundBase};
    border-radius: 100px;
  }

  #type-flying {
    background: ${(props) => props.theme.colors.typeFlyingSoft};
    color: ${(props) => props.theme.colors.typeFlyingBase};
    border-radius: 100px;
  }

  #type-psychic {
    background: ${(props) => props.theme.colors.typePsychicSoft};
    color: ${(props) => props.theme.colors.typePsychicBase};
    border-radius: 100px;
  }

  #type-bug {
    background: ${(props) => props.theme.colors.typeBugSoft};
    color: ${(props) => props.theme.colors.typeBugBase};
    border-radius: 100px;
  }

  #type-rock {
    background: ${(props) => props.theme.colors.typeRockSoft};
    color: ${(props) => props.theme.colors.typeRockBase};
    border-radius: 100px;
  }

  #type-ghost {
    background: ${(props) => props.theme.colors.typeGhostSoft};
    color: ${(props) => props.theme.colors.typeGhostBase};
    border-radius: 100px;
  }

  #type-dark {
    background: ${(props) => props.theme.colors.typeDarkSoft};
    color: ${(props) => props.theme.colors.typeDarkBase};
    border-radius: 100px;
  }

  #type-dragon {
    background: ${(props) => props.theme.colors.typeDragonSoft};
    color: ${(props) => props.theme.colors.typeDragonBase};
    border-radius: 100px;
  }

  #type-steel {
    background: ${(props) => props.theme.colors.typeSteelSoft};
    color: ${(props) => props.theme.colors.typeSteelBase};
    border-radius: 100px;
  }

  #type-fairy {
    background: ${(props) => props.theme.colors.typeFairySoft};
    color: ${(props) => props.theme.colors.typeFairyBase};
    border-radius: 100px;
  }

  #type-physical {
    background: ${(props) => props.theme.colors.typePhysicalSoft};
    color: ${(props) => props.theme.colors.typePhysicalBase};
    border-radius: 100px;
  }

  #type-special {
    background: ${(props) => props.theme.colors.typeSpecialSoft};
    color: ${(props) => props.theme.colors.typeSpecialBase};
    border-radius: 100px;
  }

  #type-status {
    background: ${(props) => props.theme.colors.typeStatusSoft};
    color: ${(props) => props.theme.colors.typeStatusBase};
    border-radius: 100px;
  }

  #type-ball {
    background: ${(props) => props.theme.colors.typeBallSoft};
    color: ${(props) => props.theme.colors.typeBallBase};
    border-radius: 100px;
  }

  #type-heal {
    background: ${(props) => props.theme.colors.typeHealSoft};
    color: ${(props) => props.theme.colors.typeHealBase};
    border-radius: 100px;
  }

  #type-repel {
    background: ${(props) => props.theme.colors.typeRepelSoft};
    color: ${(props) => props.theme.colors.typeRepelBase};
    border-radius: 100px;
  }

  #type-fleeing {
    background: ${(props) => props.theme.colors.typeFleeingSoft};
    color: ${(props) => props.theme.colors.typeFleeingBase};
    border-radius: 100px;
  }

  #type-event {
    background: ${(props) => props.theme.colors.typeEventSoft};
    color: ${(props) => props.theme.colors.typeEventBase};
    border-radius: 100px;
  }

  #type-stone {
    background: ${(props) => props.theme.colors.typeStoneSoft};
    color: ${(props) => props.theme.colors.typeStoneBase};
    border-radius: 100px;
  }

  #type-tech {
    background: ${(props) => props.theme.colors.typeTechSoft};
    color: ${(props) => props.theme.colors.typeTechBase};
    border-radius: 100px;
  }
`;

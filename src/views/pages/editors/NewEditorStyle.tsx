import styled from 'styled-components';

export const ImportProjectContainer = styled.div`
  background-color: ${({ theme }) => theme.colors.dark16};
  position: fixed;
  bottom: 0;
  width: 260px;
  padding: 24px 0;
  margin-top: auto;
`;
